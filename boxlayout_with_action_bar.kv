<BoxLayoutWithActionBar>:
    orientation: "vertical"
    ActionBar:
        ActionView:
            ActionPrevious:
                title: root.title
                # on_release: root.parent.parent.pop() # comme on a plus accès au screen manager, on doit remonter avec parent afin de pouvoir y acceder (on aurait pu faire: root.parent.manager.pop())
                on_release: app.manager.pop()
                app_icon: "images/lab_logo.png"
                app_icon_height: "28dp"
                app_icon_width: "28dp"
