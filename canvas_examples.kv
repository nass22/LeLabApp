#:set s dp(150) #permet de bind s = dp(150)

<CanvasExemplesTabs@TabbedPanel>:
    do_default_tab: False # permet de ne pas avoir la table par defaut
    TabbedPanelItem:
        text: "1"
        CanvasExample1:
    TabbedPanelItem:
        text: "2"
        CanvasExample2:
    TabbedPanelItem:
        text: "3"
        CanvasExample3:
    TabbedPanelItem:
        text: "4"
        CanvasExample4:
    TabbedPanelItem:
        text: "5"
        CanvasExample5:
    TabbedPanelItem:
        text: "6"
        CanvasExample6:
    TabbedPanelItem:
        text: "7"
        CanvasExample7:

<CanvasExample1>:
    canvas:
        Rectangle:
            # pos: dp(100), dp(200)
            # size: s, s
            pos: self.center_x-s/2, self.center_y-s/2 #permet de centrer le carré
            size: s, s
        Ellipse:
            pos: 200, 500
            size: s, s/2
        Line:
            # points: (0, 0, 100, 100) # ligne qui part de (x=0, y=0) à (x=100, y=100)
            # points: (0, 0, 100, 100, 200, 0, self.width, 100) # ligne qui part de (x=0, y=0) à (x=100, y=100) & qui redescend de (y=200 à y=0). Le self.width permet d'aller tout à droite.
            points: (0,0, self.width/4,100, self.width/2,0, self.width*3/4,100, self.width,0)
            width: 2 #rend la ligne plus épaisse

<CanvasExample2>:
    canvas:
        Line:
            points: (self.width/2, 0, self.width/2, self.height)
            width: 2
        Line:
            points: (0, self.height/2, self.width, self.height/2)
            width: 2

<CanvasExample3>:
    canvas:
        Color:
            rgb: 1, 0, 0
        Line:
            circle: (200, 200, 100) # (center_x, center_y, rayon)
            width: 2
        Color:
            rgba: 0, 255, 0, .3 # permet d'avoir une opacité de 30%
        Line:
            ellipse: (500, 300, 100, 200)
            width: 2
        Line:
            rectangle: (800, 100, 300, 200) # (x,y, width, height)
            width: 2

<CanvasExample4>:
    Button:
        text: "A"
        pos: 100, 400
        on_press: root.on_button_a_click()

<CanvasExample6>:
    # Button:
    # canvas.before:
    # #before permet de dire que l'on veut mettre les instructions du canvas avant les instructions du button (le contraire est after)
    #     Rectangle
    # text: "A"
    # pos: 100, 100 #le canvas démarre tjs à (0,0), on aura donc deux carrés. Pour éviter celà, on doit utiliser un RelativeLayout à la place du Btn.
    BoxLayout:
        size: root.size
        Widget:
            canvas:
                Color:
                    rgb: 0,1,0
                Rectangle:
                    size: self.size
        # Widget:
        RelativeLayout:
            canvas:
                Color:
                    rgb: 0,0,1
                Rectangle:
                    size: self.size
                    # pos: self.pos


<CanvasExample7>:
    RelativeLayout:
        canvas:
            Color:
                rgb: 0,0,1
            Rectangle:
                size: self.size
            
    RelativeLayout:
        canvas:
            Color:
                rgb: 1,1,1
            Rectangle:
                size: self.size
    RelativeLayout:
        canvas:
            Color:
                rgb: 1,0,0
            Rectangle:
                size: self.size
